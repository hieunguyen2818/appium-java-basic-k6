package Lesson_02;

public class PostPrefix {
    /**
     * int myNumber = 1;
     * ++myNumber -> Prefix
     * --myNumber -> Prefix
     * myNumber++ -> PostFix
     * myNumber-- -> PostFix
     *
     * num1 = 1
     * num2 = 2
     *
     * int total = ++num1 + num2++
     * -> total number = ? = 4
     * -> num1 = ? = 2
     * -> num2 = ? = 3
     */
}
